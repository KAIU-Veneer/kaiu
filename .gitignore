node_modules/
dist/
.vite/
.vercel/

# Blender exports. Too large for GitHub (100 MB file limit); run
# `npm run model` to produce the web-ready public/models/room.glb.
models-src/

# Full-size project photographs. The site ships the WebP versions in
# public/assets/projects; keep the originals here, out of the build.
photos-src/

# Secrets: never commit real values, only .env.example
.env
.env.*
!.env.example

.DS_Store
*.local
npm-debug.log*
